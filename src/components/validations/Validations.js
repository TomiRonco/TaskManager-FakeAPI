import React, { useState } from 'react'

const Validations = ({name, lastName, userName, email, phone, password, confirmPassword}) => {
    const [errors, setErrors] = useState ({name: "", lastName:"", userName:"", email:"", phone:"", password:"", confirmPassword:""})
    
  return (
    <>
    if (errors === ""){
      
    } 
    </>
  )
}

export default Validations
